1. Checkout "master" branch
2. Open intellij and import the project
3. run mvn -U install in the terminal to download the dependencies
4. run mvn clean verify to run the tests and generate Serenity Reports
5. Serenity reports are generated at "/target/site/serenity/index.html"
